# --- FUNCIONALIDADE ESSENCIAL DO PROJETO ---
RewriteEngine On
RewriteBase /

# Redireciona os pedidos da API para a pasta correta do backend
RewriteRule ^api/(.*)$ /Painel-Informativo/server/api/$1 [L,QSA]